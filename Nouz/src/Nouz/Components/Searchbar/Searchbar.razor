@inherits SharedComponentBase

<div class="searchbar">
    @if (ShowNotesControls)
    {
        <RadzenIcon Icon="search"
                    Style="color: var(--text-muted);" />
        <input type="text"
               placeholder="Search notes..."
               value="@_query"
               @oninput="HandleSearchInput" />
        <RadzenButton Icon="close"
                      Variant="Variant.Text"
                      Style="@($"color: var(--text-muted); visibility: {(string.IsNullOrEmpty(_query) ? "hidden" : "visible")}")"
                      Click="@(() => ClearSearchInput())"
                      title="Clear search" />
    }

    <div class="searchbar-spacer"></div>

    @if (ShowNotesControls)
    {
        <RadzenButton Icon="add"
                      Variant="Variant.Text"
                      Style="color: var(--text-muted);"
                      Click="@CreateNote"
                      title="New note" />
        <RadzenButton Icon="calendar_add_on"
                      Variant="Variant.Text"
                      Style="color: var(--text-muted);"
                      Click="@CreateMeeting"
                      title="New meeting note" />
        <div class="searchbar-divider"></div>
    }
    <RadzenButton Icon="settings"
                  Variant="Variant.Text"
                  Style="color: var(--text-muted);"
                  Click="@OpenSettings"
                  title="Open settings" />
</div>